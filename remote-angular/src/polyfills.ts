/**
 * Zone JS is required by Angular itself.
 */
import 'zone.js';

/**
 * Required to support Web Animations.
 * Needed for Angular when using AnimationBuilder.
 */
// import 'web-animations-js';

/**
 * By default, zone.js will patch all possible macroTask and DomEvents
 * user can disable parts of macroTask/DomEvents patch by setting following flags
 */
// (window as any).__Zone_disable_requestAnimationFrame = true;
// (window as any).__Zone_disable_on_property = true;
// (window as any).__zone_symbol__BLACK_LISTED_EVENTS = ['scroll', 'mousemove']; 

/**
 * Web Components polyfills for older browsers
 */

// Uncomment these lines if you need to support older browsers
// import '@webcomponents/webcomponentsjs/custom-elements-es5-adapter.js';
// import '@webcomponents/webcomponentsjs/webcomponents-bundle.js'; 